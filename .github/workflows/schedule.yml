name: Curl Every 5 Minutes

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  curl_and_output:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run Node.js application
        run: npm start
